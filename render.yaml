services:
  - type: web
    name: jules-api-interface
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: JULES_API_KEY
        sync: false